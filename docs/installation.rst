============
Installation
============

At the command line::

    $ easy_install django-sockpuppet

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-sockpuppet
    $ pip install django-sockpuppet
